<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleDevelopmentRegion</key>
    <string>en</string>
    <key>CFBundleLocalizations</key>
    <array>
        <string>en</string>
    </array>
	<key>CFBundleURLTypes</key>
	<array>
		<dict>
			<key>CFBundleTypeRole</key>
			<string>Viewer</string>
			<key>CFBundleURLName</key>
			<string>com.machinespirit.leader-key</string>
			<!-- machine-spirit fork: `msleaderkey` is OURS alone, so app->fork
			     pings (#36/#37) route to this running instance. Bare `leaderkey`
			     collides with stock Leader Key and LaunchServices resolves it to
			     /Applications/Leader Key.app, so it can't reach the fork. -->
			<key>CFBundleURLSchemes</key>
			<array>
				<string>msleaderkey</string>
				<string>leaderkey</string>
			</array>
		</dict>
	</array>
	<key>SUFeedURL</key>
	<string>https://mikker.github.io/LeaderKey.app/appcast.xml</string>
	<key>SUPublicEDKey</key>
	<string>4ZfPYKvzE7b/uYW4B8f2v++7Bry/OmvYHCzl3g936KE=</string>
	<!-- machine-spirit fork: the feed above is STOCK Leader Key's appcast, so
	     leaving auto-update on would silently "update" this fork back INTO stock.
	     The fork is built from source (forks/LeaderKey) and versioned in-repo, so
	     automatic checks are OFF by default. (Manual "Check for Updates" still
	     hits stock's feed — a footgun to retire once the fork has its own feed.) -->
	<key>SUEnableAutomaticChecks</key>
	<false/>
	<!-- machine-spirit fork: single-instance so URL-scheme events (the app->fork
	     pings, #36/#37) route to the running daily driver instead of launching a
	     fresh copy that traps in AppKit window-restoration. -->
	<key>LSMultipleInstancesProhibited</key>
	<true/>
</dict>
</plist>
