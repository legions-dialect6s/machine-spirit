# ---- Secrets: never commit these to a PUBLIC repo ----
.env
.env.*
*.pem
*.key
*.p8
*.p12
*.mobileprovision
id_rsa*
id_ed25519*
*_rsa
*_ed25519
.netrc
.npmrc
credentials
credentials.*
secrets.*
*.secret
# Common secret-bearing dotfiles — commit SANITIZED copies only, never live ones
.zsh_history
.bash_history
.aws/
.ssh/
.gnupg/
.config/gh/hosts.yml

# ---- macOS cruft ----
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*

# ---- Editor / tooling ----
*.swp
*.swo
node_modules/
.venv/
__pycache__/
*.pyc

# ---- Compiled artifacts (built by install.sh, never committed) ----
bin/shield-fx           # busy-pane shield overlay, compiled from assets/tools/shield-fx.swift
